apiVersion: apps/v1
kind: Deployment
metadata:
  name: vote
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vote
  template:
    metadata:
      labels:
        app: vote
    spec:
      containers:
        - name: vote
          image: shorya1853/vote:latest
          imagePullPolicy: Always
          env:
            - name: DB_HOST
              value: postgres
            - name: DB_NAME
              value: voting
            - name: DB_USER
              value: user
            - name: DB_PASS
              value: password
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: vote
spec:
  selector:
    app: vote
  ports:
    - port: 80
